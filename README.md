prelude-personal-scripts
========================

The directory for personal scripts under Emacs Prelude

Usage:

    cd ~/.emacs.d/personal
    git init
    git remote add origin https://github.com/crlf0710/prelude-personal-scripts.git
    git pull
    git checkout master
